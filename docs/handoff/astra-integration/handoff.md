# Application integration handoff

**Last Updated (UTC):** 2026-09-13T13:10:37.091677+00:00
**Status:** In Progress
**Current Focus:** Verify every supported application component and the research-to-report context chain, then repair confirmed gaps.

## 1) Request & Context
The user asks whether each component works and whether research, ontology, knowledge graph, multi-agent simulation and final report are connected efficiently without losing context. This takes priority over the pending monetary-reservation slice. The implementation baseline is ef8c419 in /Users/rogerlin/.codex/worktrees/drf-astra-improvements. The original checkout has separately owned frontend cleanup and remains read-only except its ignored append-only continuity file. No live provider calls, pipeline launch/resume, deployment, credentials or saved-run mutation are authorized by this offline audit. Existing source and actor seals, explicit evidence gaps and diagnostic forecast limits must remain intact. The prior request authorizes routine verified fixes; any new broad capability must first have a concrete design.

## 2) Requirements → Acceptance Checks
| Requirement | Scenario check | Expected outcome | Evidence |
|---|---|---|---|
| Every supported component works | Run backend tests, frontend unit/build and offline Flask/API smoke checks | Failures reproduced and repaired or explicitly classified | Durable command/log/JUnit receipts |
| Context flows across all six stages | Trace real producers and consumers; carry distinctive source/actor/context markers through offline stage boundaries | Relevant context arrives with identity and provenance intact | Source-linked matrix and integration regressions |
| Resume/reuse preserves valid context | Exercise stale manifests, mismatched lineage and tampered actor artifacts | Reject invalid reuse without laundering or silently losing context | Existing and new negative contract tests |
| Context transfer is efficient | Inspect bounded prompts, selective reads, reuse and duplicate extraction | Confirmed unnecessary work removed with comparative evidence | Targeted counts/timings and preserved outputs |
| Scope is accurately reported | Separate source traces, stubs, actual local services and live providers | No claim of untested production integration or global optimality | Final verification report |

## 3) Plan & Decomposition
First map current stage contracts and run the baseline gates in parallel. Use each failed assertion or lost context marker to select one coherent fix at a time. Add regressions before fixing confirmed defects; then run affected and integrated checks. Finish with an independent source/evidence review, updated recommendations/ledger and local commits. The hourly improvement loop must not start a conflicting monetary slice while this user-directed audit is active.

## 4) To-Do & Progress Ledger
- [x] Rechecked clean implementation Git state and original checkout ownership.
- [x] Read previous continuity, current architecture memory and verification workflow.
- [x] Trace research/ontology/graph contracts and tests; confirmed issues remain individually tracked.
- [x] Trace PREPARE/child/native agent context and tests; shared denial repaired, Twitter final attestation remains open.
- [x] Trace simulation/report/output/frontend contracts and tests; report-context and UI acceptance follow-ups remain open.
- [ ] Run application baseline gates and repair confirmed defects sequentially.
- [ ] Produce cross-stage verification matrix and context-transfer scenario evidence.
- [ ] Independently review, update continuation state and commit.

## 5) Findings, Decisions, Assumptions
The active authority is PipelineOrchestrator and pipeline_state.json; drf2 remains comparison scaffolding until source proves otherwise. Original frontend cleanup is separate from the isolated branch and must not be overwritten. Offline integrations can verify artifact transfer and mocked API behavior, but cannot establish live provider health or forecast quality. Fresh source tracing will verify these assumptions.

## 6) Issues, Mistakes, Recoveries
No new issue is confirmed at intake. Prior GitHub authentication rejection remains unchanged; local work proceeds without retrying publication. Existing price coverage is verified, while full monetary reservations remain pending.

## 7) Scenario-Focused Resolution Tests
Baseline and cross-stage scenarios are in progress. Every confirmed defect will retain a reproducer, correction and post-change result here and in the verification receipt.

## 8) Verification Summary
No fresh test result is claimed at intake. Evidence will be retained under /Users/rogerlin/.codex/astra-evidence/integration-20260909 and sanitized source-linked summaries under docs/research/.

## 9) Remaining Work & Next Steps
The immediate critical path is source-contract mapping plus broad offline baseline gates. Fix existing broken integration before optimizing transfer overhead. Resume the monetary-reservation backlog after the user-directed application check is complete.

## 10) Updates
- 2026-09-09T14:41:58.171164+00:00: Created task brief, requirements, ownership and acceptance plan before code changes. Parallel read-only specialist scopes are upstream artifacts, simulation context and report/output context; root owns integration gates, fixes, synthesis and commits.

- 2026-09-09T14:48:01.500847+00:00: Frontend baseline passes: implementation113 unit cases and build4.91s; original separate cleanup69 cases and build4.39s. Both builds use Vite native config loading and external output directories to avoid writing through shared node_modules. Broad network-blocked backend suite is still running with two failures observed, details pending final report. Simulation review independently passed156 relevant cases but reproduced a missing negative scenario: actor_context gives explicit actor_knows=false precedence over public visibility, whereas shared-world selection ignores that denial and reinjects the claim to every actor. Select the narrow access-precedence repair first. Reviewer owns simulation_config_generator.py and a new regression file; red tests may be written now, runtime source waits for baseline completion. Twitter final-context attestation and repeated per-actor report/contract preprocessing remain candidate follow-ups.

- 2026-09-09T14:49:34.883666+00:00: Upstream review passed 135 cases and reproduced two additional defects. A 162,047-character safe report retains its tail in graph chunks but loses it in the actual ontology prompt because per-document sanitation uses the final 120,000-character presentation cap before head/middle/tail sampling. Separately, the real orchestrator reused a modified ontology even though _reuse_ok rejected it, then rewrote its manifest hash during stage completion. Registered local issues 01/02/03 for actor access, ontology sampling and ontology reuse. Root owns reuse design; upstream specialist owns new sampling red tests, awaiting runtime-edit signal. Research/graph contracts otherwise enforce fetched receipts, exact lineage and strict-v1 graph seed readback. Intentional bounded projections/cast filtering are recorded as tradeoffs. An initial documentation append ran from the browser-evidence directory and failed to locate the ledger; repeated with absolute project paths. No source was affected.

- 2026-09-11T13:17:49.866526+00:00: Resumed ASTRA-INTEGRATION-01 from unchanged ef8c419. Only root owns runtime edits. Independent scopes are external test-launcher repair, saved-run aggregate refresh, and the user's recurring-workflow inventory. Benefit hypothesis: applying the existing literal-false precedence before shared-row rendering prevents denied research knowledge entering all actors and avoids rendering those denied rows, without provider work or artifact rewriting. Fresh regression: eight denial cases fail at all six shared consumers; four public controls pass. The draft's two public-without-grant actor-local assertions were corrected after reading ACTOR_KNOWN_VISIBILITIES; public sharing and pre-existing actor knowledge are distinct contracts. No fixture budget or runtime contract was loosened.
- Test infrastructure correction: the September 9 broad launcher omitted a main guard, so multiprocessing re-entered pytest and overwrote shared receipts. Its reported broad counts are invalid and are retained only for diagnosis. The independent recovery uses a guarded launcher and new phase files; no application process failure is inferred from those nested runs.
- Confirmed follow-ups retained from the component trace: alternate report constructors omit five research artifacts; broad TypeError retries can silently discard context; report chat does not inject constructor research context; coalition_map takes only the last 100,000 already-loaded actions. Source-only follow-ups are Twitter final-context attestation and repeated actor/action preprocessing. These remain separate verified slices.

- 2026-09-11T13:21:13.785437+00:00: Initial actor-denial implementation passes 168 focused context/config/role/cast/seal/runtime cases (actor-green.xml), and both modified actor Python files pass Ruff/compilation. Independent review is in progress; the full actor feature remains passes=false because other cross-stage acceptance gaps remain. Saved refresh confirms all77 artifact hashes,32 states and12 bounded application logs unchanged on September11; this is historical coverage, not a production test of this fix.
- The pending ontology reproducer independently returns five failures and one pass (ontology-pending-red.xml). Retained its five known failures as strict xfails with ASTRA-INTEGRATION-02 reasons so the next slice has permanent acceptance cases; unexpected success will force removal of the markers. This explicitly records outstanding defects and does not count them as passing.
- Corrected harness process recovery returns ten passes and one known drf2 scaffold path failure. The ten process regressions require no runtime repair. The remaining test expects the current worktree skills path, whereas checked-in drf2/config/config.yaml intentionally pins the original checkout path per its README. Record as portability debt; do not silently retarget the scaffold configuration during the actor-access slice.

- 2026-09-11T13:25:12.613293+00:00: Independent review rejected the first actor fix: a public duplicate in another actor pack still reintroduced denied knowledge. Four permanent cases reproduce this across both actor orders and two dimensions (4fail/12pass before correction), with case-folded duplicates. Final selector collects denied identities across validated packs and removes matching shared candidates after the full walk. The claim identity uses the existing sanitized, case-insensitive deduplication rule. Authorized actor-local knowledge and modeler audit remain intact. This changes the earlier early-render-skip design: sanitization is required to identify denied copies; the benefit is correct context selection, not a claimed runtime speedup. Fresh guarded actor gate172pass7.23s. Independent final review/comparison and broad application gate follow.

- 2026-09-11T13:29:11.817727+00:00: First corrected whole-backend run completed: 3893passed/4failed/17skipped/16xfailed in137.09s. Three failures are URL-validation tests using real getaddrinfo; the strict offline DNS guard correctly blocked them. Assigned a narrow deterministic resolver fixture in test_utils_security.py, preserving product security behavior and existing assertions. The remaining failure is the documented drf2 path mismatch. A fresh whole gate will follow the fixture repair; do not label this run green.
- Fresh frontend gate113pass, build2.32s using native Vite configuration loading and external output. The separate original checkout frontend cleanup remains intact. Runtime and two actor/ontology regression files compile and pass Ruff.
- The additional user request to find repeated workflows was fulfilled with a recent-task-first inventory. Three paper-review requests across September7–8 support one missing paper-review-evidence skill, now created under ~/.codex/skills and structurally validated. Existing forensics, recovery, UI skills and the active ASTRA automation cover the other high-confidence workflows; cross-stage packaging waits for a stable audit procedure. Private manuscript text was not copied. Shortlist and creation evidence are in the external evidence vault. No existing skill, custom agent, automation or memory was modified.

- 2026-09-11T13:31:32.550497+00:00: Independent final review approves the scoped actor-access repair with18passing cases and no blocking finding. Exact-selector paired comparison against ef8c419 produces4/16→16/16 passing scenarios and71→0 denied case/channel observations, preserving24/24 positive observations and all16 pack sets. The seed-first-row policy explains why the baseline is71 rather than72. Saved comparison/review/run-refresh JSON are now repository artifacts under docs/research/astra-integration-*.json. The full offline gate is rerunning after deterministic security-test resolver fixtures; no runtime security policy changed.

- 2026-09-11T13:33:46.827991+00:00: ASTRA-INTEGRATION-01 is verified offline and independently approved. Shared literal-false denials now veto duplicate global claims across participating validated packs; source/actor seals, authorized actor knowledge and modeler audit remain intact. Final gate:3896pass/1known drf2 scaffold path failure/17skip/16xfail (5new ontology defects); focused172pass; independent18pass; frontend113pass and build2.32s; four changed Python files pass Ruff/compilation. Final whole gate records69 guarded Python processes, zero network events and no source drift. Paired exact-selector comparison71→0 denied case/channel observations,24/24positive controls and16/16unchanged pack sets. No production speedup or savings claim. Source hashes, receipts, comparison and limits: docs/research/astra-integration-verification.json.
- Next-session prompt: Read docs/handoff/astra-integration/handoff.md, PLANS.md and astra-improvement-state.json. Continue the active application audit with issue02: sanitize full documents before ontology sampling, including normalization/replacement expansion. Start from the five permanent strict-xfail scenarios, remove markers when they pass, verify real bounded prompt content and adjacent research/graph contracts, independently review and commit. Then address issue03 ontology reuse/input binding plus dependent invalidation before report entry-point/context repairs04/05 and full-action coalition completeness06. Keep feature_actor_intelligence_grounding_v2 passes=false until all its cross-stage acceptance boundaries hold. Do not start monetary reservations while this user priority remains active. The fixed external test launcher uses a main guard, new phase paths and inherited offline protection; do not reuse the invalid September9 launcher. Preserve the one known scaffold portability failure and11existing xfails distinctly from the five current ontology defects. No provider, real pipeline, original cleanup, saved artifact, deployment or rejected-auth retry is authorized by this offline continuation.

- 2026-09-11T13:35:33.406380+00:00: Committed the independently approved actor-access repair,16permanent actor scenarios, five explicitly pending ontology xfails, deterministic security-test DNS fixtures, boundary matrix and sanitized evidence as `ef4f795fd7588df7ae16e89ddd34c41e7677bf87` (`fix: preserve actor access denials across shared context`). The isolated worktree was clean immediately after that commit. The full integration audit remains in progress and the hourly automation remains useful; next issue02. GitHub publication was not retried because the existing authentication rejection has no changed evidence.

### ASTRA-INTEGRATION-02 complete sanitation before selection — 2026-09-13T11:33:45.351316+00:00

Selected the next authorized repair from clean8cdd953. The five existing strict-xfail ontology scenarios are the acceptance baseline. Benefit hypothesis: retaining the complete sanitized evidence corpus until head/middle/tail selection restores late actor/relationship context without increasing the final ontology prompt budget or changing source artifacts. The sanitizer already inspects the entire input before truncating; an explicit max_chars=None mode can retain its complete result, avoiding unreliable guessed expansion caps while preserving capped/default behavior. Root owns actor_role_prompt.py, ontology_generator.py and permanent regressions. A read-only specialist checks whether graph preprocessing shares the same Unicode/CR-expansion loss, which would be repaired as a directly connected consumer of this same boundary. A separate forensic specialist refreshes historical hashes and bounded aggregates.

Acceptance: reproduce the five current failures without xfail suppression, verify full sanitation handles Unicode normalization and omission-marker expansion, retain source markers at relevant ontology sample positions, remove unsafe multi-line controls before slicing, preserve short/capped existing output and public trust delimiters, and exercise actual ontology generate primary/fallback request construction with a fake client. Keep the existing presentation limits; no live provider or pipeline is needed. Measure retained context and local prompt size/time against exact8cdd953 behavior, independently review the source and evidence, run affected plus broad offline gates, then commit one coherent fix. Ontology reuse/input freshness and dependent invalidation remain issue03 and are not changed in this slice.

- 2026-09-13T11:37:29.669010+00:00: Fresh red ontology baseline5failed/1passed using --runxfail, then initial fix64focused passes and8actual ontology prompt/generate cases pass. The shared sanitizer accepts explicit None only to retain its complete safe result; default and capped calls remain unchanged. Primary and real template fallback both retain head/middle/tail markers, remove cross-line instructions and keep document cap+400 and8192 output-token setting.
- Independent exact8cdd953 graph reproduction confirms the directly connected expansion defect:1046raw chars become18046safe chars against a1238legacy cap;7046raw CR-only chars become47045safe chars against7238cap. Both lose safe tails. Extend this same full-sanitation repair to graph preprocessing, leaving chunk size/overlap, image stripping and per-episode delimiters unchanged. Added two permanent bounded-episode tail regressions. Restored graph evidence can increase episode/provider work; do not claim a runtime or Pareto speedup.
- Fresh September13forensics confirms77artifacts/32states/12boundedlogs unchanged, no newpipelineIDs/newerselectedlogs and zero log reparses. Historical ontology spans44.616705/13560.966091/42.409762/60.201604s do not isolate processing time; the long one overlaps restamped downstream stages. No post-change production evidence is available.

- 2026-09-13T11:46:52.662469+00:00: ASTRA-INTEGRATION-02 verified offline. Full sanitized corpus now reaches ontology sampling and graph splitting, including NFKC/CR-only expansion; existing finite document/chunk envelopes remain. All five ontology strict-xfail markers were removed after passing their original scenarios. Twelve ontology/graph boundary cases pass, and independent review passes 69 cases with no blocker. Final whole backend gate: 3,905 passed, one known drf2 scaffold path failure, 17 skipped and 11 pre-existing xfailed in 142.73s. All five touched Python files pass Ruff and compilation. Final gate recorded 69 guarded Python processes, zero network events and no source drift. Exact-baseline comparison verifies 56 capped/default byte matches, short prompt/episode parity and unchanged primary/fallback request settings. Long-report ontology markers improve 2/3 to 3/3; graph expansion examples restore episodes 3→34 and 14→87 at a 600/60 chunk policy. Three-sample local long-report prompt medians 1.55392s→1.55618s support no production speedup claim; intermediate memory and restored graph work can increase. Evidence: docs/research/astra-ontology-context-verification.json and linked comparison/review/compact saved refresh.
- Next-session prompt: Continue the active integration audit from this verified slice. Read docs/handoff/astra-integration/handoff.md, PLANS.md and astra-improvement-state.json, recheck Git/ownership, then select ASTRA-INTEGRATION-03. Reproduce the ontology shortcut accepting a changed registered artifact or stale research inputs. Design explicit input binding and reuse validation, plus invalidation of dependent GRAPH/PREPARE/RUN/REPORT reuse when ontology changes; preserve legacy semantics with evidence rather than silently reblessing old artifacts. Add current, stale, tampered and regeneration/reuse tests before repair, verify offline with the corrected spawn-safe launcher and existing source/actor contracts, independently review and commit one coherent slice. Keep all other feature statuses unchanged and full actor feature false. Report entry-point/chat closure04/05 and coalition completeness06 follow; monetary reservations stay queued. Preserve original edits, saved artifacts, providers and credentials. No real pipeline launch/resume, provider call, restart, deployment or unchanged authentication retry is authorized.

- 2026-09-13T11:48:32.246752+00:00: Committed the independently approved full-sanitation repair and its ontology/graph acceptance evidence as `9ab66777c50a5ab4efc5510b61799267b8e42b58` (`fix: preserve complete research before ontology sampling`). The worktree was clean immediately afterward. This records issue02 complete offline, with next issue03 ontology reuse/input binding and dependent invalidation. The broader application audit and hourly loop remain active. Remote publication was not retried because the documented authentication rejection has no changed evidence.

### ASTRA-INTEGRATION-03a ontology reuse integrity fence — 2026-09-13T12:43:10.955715+00:00

The live source trace reproduces four gaps: corrupted registered ontology is reused and re-registered; project/file mismatch is accepted; changed research can reuse old ontology; and regenerated ontology can restore old downstream stages. Safe automatic rebuilding additionally needs durable identity invalidation, derived-file archival or generation binding, ensemble checkpoint closure, and copy-on-write ownership for scenario forks sharing the base project/handoff. Stage-bit resets alone would corrupt that contract.

Select one prerequisite,03a: when artifact validation is enabled, require the cached project ontology to agree with the handoff JSON and available registered path/hash/size proof before downstream work. Read and validate the same file bytes. On failed proof, stop the current attempt in ONTOLOGY without generating, restoring downstream results, rewriting the ontology or replacing its integrity entry. On healthy reuse, preserve the original registration rather than recording a new fingerprint from a later disk read. Retain the existing validation opt-out and legacy project-only absence behavior explicitly; neither establishes input freshness. This is a correctness fence, not automatic regeneration or full03 completion.

Root owns production changes in pipeline_orchestrator.py. The test specialist may own a new test_ontology_reuse_integrity.py and the minimal optional fixture hook needed for real _run regressions; no other test assertions will be weakened. Independent review checks TOCTOU, fork/base preservation, disabled/legacy behavior and absent/malformed evidence. Acceptance covers tampered/missing/foreign/malformed ontology, project mismatch, healthy exact reuse, late file replacement without re-registration, unchanged source bytes and no downstream work on denial. Compare baseline and current state-machine outcomes/ontology-registration writes, then run focused and broad offline gates. The existing input-binding and downstream invalidation failures remain03b/03c with their reproductions retained. No saved run, provider, deployment, restart or authentication retry is part of this offline repair.

- 2026-09-13T12:54:43.595390+00:00:03a implementation and 13 permanent real-state-machine scenarios now pass with the existing wiring suite (68 total). The guard uses one ontology byte read for metadata, JSON/schema and semantic project comparison. It runs before RESEARCH bookkeeping and again at reuse because the general manifest loader otherwise treats malformed JSON as empty and would overwrite that original evidence. Reused ontology completion preserves the prior registration; valid deep links remain. Malformed-manifest, missing-hash legacy, validation-disabled, legacy-project-only, wrong-path and late-replacement cases preserve their explicit acceptance contracts. The feature does not regenerate invalid cached ontology or establish input freshness. All three changed Python files pass Ruff/compilation. Independent review/comparison and full backend gate follow.

- 2026-09-13T12:57:33.951795+00:00: Precommit source review found a fork-specific03a integrity gap. Ontology reads state.handoff_dir (the shared base for a scenario fork), while PipelineManager.artifact_manifest_path uses the fork's static local handoff. The first guard could therefore miss the base's registered hash and accept a semantically equal file whose bytes changed. Extend the same-byte check to all available registrations in both current-pipeline and actual ontology handoff manifests, deduplicating identical paths, without writing to either. The test specialist is adding owner-manifest and conflicting-child regressions before runtime correction. The current broad run predates these final tests/source and will be retained separately; final gate must follow the correction. Full03b/03c remains pending.

- 2026-09-13T13:03:03.613388+00:00: The fork regression reproduced four failures and one positive control before correction. The final guard now verifies both the current pipeline's registration and the actual ontology handoff owner's registration, deduplicating equal paths and checking all available constraints against one byte snapshot. This prevents child metadata from overriding an invalid owner proof. All18 permanent cases plus55 wiring tests pass (73 total); allthree Python files pass Ruff/compilation. The initial broad gate3918pass/1knownfail/17skip/11xfail is preserved as evidence for the earlier version; the final broad gate follows the owner-proof correction.

- 2026-09-13T13:10:37.091677+00:00: ASTRA-INTEGRATION-03a is independently approved and verified offline. Final guard covers every available current-pipeline and shared-owner registration against the same ontology bytes, project identity and schema, before RESEARCH bookkeeping and at reuse. Original registration survives healthy reuse and late replacement; invalid proof stops ONTOLOGY without regeneration or downstream entry. All18 permanent tests and55 existing wiring tests pass; independent review82pass, no blocker. Broad gate3,923pass/1known drf2 scaffold path failure/17skip/11existingxfail in148.45s,69guarded processes,zero network events and no source drift. Three Python files pass Ruff/compilation. Paired exact5d7b147 _run+_complete_stage comparison: four invalid completions→zero;16downstream stage entries→zero; valid/late-replacement registration behavior corrected. Warm82-byte local guard median0.205ms/21checks is validation overhead, not production speedup. Evidence: docs/research/astra-ontology-reuse-verification.json.
- The first independent comparison copied module globals, so module-name service replacements missed the baseline function. The guard blocked12DNS attempts during real GraphBuilder client construction; the owned launcher was interrupted and its processes exited. No outbound request succeeded. That phase is invalid evidence. Rebinding exact baseline code to live globals restored identical mocks; final independent evidence has zero network events. Class-attribute ProjectManager/storage mocks remained effective. This guardrail failure and correction are retained externally.
- Next-session prompt: Continue the active application audit from03a, without marking parent03 or the actor feature complete. Read this handoff, PLANS.md, ledger, and the external ontology-reuse-20260913/downstream-invalidation-map.md plus four full03 baseline reproducers. Design03b/03c coherently: bind ontology to effective report/dossier/actors/prompt/generation policy, make new ontology/input-proof publication strict, and establish durable owned invalidation before any regeneration. Preserve old identities and canonical derived files; account for PREPARE's missing graph-ID comparison, REPORT lookup by simulation ID, ensemble_done/checkpoint lineage, graph enrichments that write only when nonempty, and forks sharing base project/handoff. A simple stage reset or unconditional new file write is insufficient. Define explicit legacy/fork policy without silently blessing absent proof, then implement dependency-ordered regressions for crash windows, stale input, empty new graph outputs and base immutability. The current integrity fence deliberately rejects invalid cached data; do not bypass it while adding regeneration. Report context04/05 and coalition completeness06 follow; monetary reservations remain queued. No provider, real run/resume, restart, deployment, saved artifact mutation or unchanged-auth retry is authorized by offline continuation.
