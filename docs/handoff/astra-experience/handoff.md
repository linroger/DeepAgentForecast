# ASTRA experience and complete successful-run audit

**Last Updated (UTC):** 2026-09-16T12:30:36.676096+00:00
**Status:** In Progress
**Current Focus:** Source review and binary-market identity/provenance repairs; graph06a is verified, browser automation remains stopped.

## 1) Request & Context
The user requests a careful whole-codebase study, an audit of every successful past run including each stage and available logs, practical fixes for token/time waste (reported above150million tokens and6hours), a direct localhost DeepResearchForecast entry, a more professional UI, improved visualizations and Polymarket integration, better cross-stage context, and continued iteration. This expands the existing ASTRA program rather than replacing its unresolved integration/accounting work. Implementation stays in the isolated worktree on codex/astra-workflow-review-2026-09-07, baselineb2a2e10. Original checkout cleanup remains separately owned and must be preserved. Existing automation was found PAUSED and has now been explicitly resumed ACTIVE under the user's renewed loop request, with expanded instructions. No duplicate automation is created.

## 2) Requirements → Acceptance Checks
| Requirement | Scenario check | Expected evidence |
|---|---|---|
| Audit every successful saved run | Inventory all completed parent/child runs and every stage, reconcile counters and available logs | Per-run/stage JSON coverage matrix, sanitized aggregates and hashes, explicit missing data |
| Explain high token/time consumption | Rank reconciled stage costs and critical-path intervals, inspect retries and repeated work | Exact accounting scope, non-overlap rules, source-backed mechanisms and uncertainty |
| Direct localhost entry | Load root, /research alias, refresh, navigation, brand click | Research UI remains primary with no accidental legacy navigation or duplicate launch |
| Professional, accessible UI | Inspect desktop/mobile layouts, keyboard controls, pending/error states | Before/after screenshots, functional browser acceptance, frontend tests/build |
| Better visualizations and market integration | Trace research/report/API/UI contracts and reproduce specific defects | Source-linked issues, controlled data scenarios, one verified repair at a time |
| Persistent implementation | Maintain current priorities and iterate through justified fixes | ACTIVE existing loop, small owned commits, updated recommendations/handoffs |

## 3) Plan & Decomposition
Parallel read-only audits cover all successful runs, UI/launcher integration and visualization/Polymarket context. Root owns the first coherent UI entry/workspace slice, preserving existing launch-intent and usage behavior. Inspect a rendered baseline, then implement a focused design with clearer hierarchy, concise product copy, consistent surfaces/controls and keyboard navigation. Validate in an isolated loopback fixture with no real backend proxy. Use audit evidence to prioritize the next workflow or visualization/market repair. Do not mix unrelated speculative changes into one commit.

## 4) To-Do & Progress Ledger
- [x] Rechecked clean isolated Git state and original checkout's separate dirty frontend work.
- [x] Resumed the existing hourly improvement loop with the expanded user scope.
- [x] Complete all-successful-run inventory, per-stage costs and log coverage;26pipelines/156stages/254logs verified.
- [x] Direct-entry/workspace/readiness/launcher slice verified offline; source-only work continues.
- [x] Source audit and negative reproducers registered; visualization/market repairs remain open.
- [ ] Independently review, run relevant gates, update recommendations and commit.

## 5) Findings, Decisions, Assumptions
Both checkouts already route '/' to ResearchView. The isolated branch still sends its brand click to the legacy Home route, whereas the original cleanup has removed those legacy routes. Port intentional behavior selectively while retaining ASTRA launch/usage work; do not copy whole files over divergent changes. Historical runs may predate current fixes, and reported totals may mix cumulative/child counters; evaluate the user's figures against complete saved evidence without assuming either correctness or overcount.

## 6) Issues, Mistakes, Recoveries
The hourly automation was paused, contrary to the previous handoff's active-at-that-time status; fresh inspection found this and the current user request authorizes reactivation. Existing known drf2 worktree-path failure remains visible. Remote publication remains blocked by prior authentication rejection without changed evidence.

## 7) Scenario-Focused Resolution Tests
Use the existing isolated launch fixture and browser acceptance patterns to test root routing, brand navigation, bilingual presentation, form controls, pending launch recovery, responsiveness and keyboard access. No real research or simulation run is required. Per-run forensic checks must prove coverage and reconciliation rather than equating missing telemetry with zero work.

## 8) Verification Summary
Evidence root: /Users/rogerlin/.codex/astra-evidence/experience-20260916. Browser artifacts use output/playwright within the implementation worktree, then verified screenshots/receipts may be retained in the external evidence vault. Fresh gates are pending; prior3923-pass backend result remains historical.

## 9) Remaining Work & Next Steps
Complete the read-only audits alongside the UI slice, then choose the largest justified next workflow defect. Existing ontology input-binding/invalidation03b/03c, report-context04/05, coalition completeness06 and monetary admission remain open. Do not restart healthy services, run/resume paid pipelines, call providers, deploy, mutate saved artifacts or change secrets without explicit authorization for that action.

## 10) Updates
- 2026-09-16T10:35:18.838753+00:00: Created expanded brief, full acceptance matrix and parallel ownership before source changes. User clarified that every successful run and each stage/log should be audited. Root retains runtime/UI implementation ownership until assigning a bounded worker scope.

- 2026-09-16T10:40:41.872182+00:00: Captured1440×1080 baseline screenshot in output/playwright/astra-before-desktop.png using the isolated fixture on127.0.0.1:18743. Neither3000 nor5001 was listening at inspection. Assigned ResearchView/router plus focused preflight helper/tests to a UI worker; root owns browser acceptance and documents. The original cleanup must not be copied wholesale because it predates ASTRA launch-intent and usage changes. New source-backed issues registered: stale graph response, false-ready/error and readiness races, Vite/launcher port/open mismatch, hidden-panel polling, research/current market quote conflation, blank token positional shift, invalid resolution acceptance, missing-value probability coercion and invalid history handling. Market agent reproduced these against actual functions and passed243backend plus21frontend existing checks; those green tests did not cover the new negative scenarios. Full cost audit now covers every successful saved run as explicitly requested.

- 2026-09-16T10:42:39.852662+00:00: Assigned launcher/proxy connection fixes to the UI-entry specialist as a separate file ownership scope within the same direct-entry feature. Vite currently independently opens a browser and hardcodes5001 despite the launcher acceptingFLASK_PORT. Preserve one browser-opening owner and explicit port agreement; root will validate through isolated services. Full UI design worker remains on ResearchView/router only.

- Complete successful-run audit delivered:26completed pipelines,156stage rows,254runtime logs/137,010,574bytes and597source receipts. Every successful ID has some log coverage; per-stage missing attribution remains explicit. All saved run creation dates remainJune8–July15. Reference f235 records79,749,778research tokens (96.10%parent), rawGRAPH/RUN unmetered, longestGRAPH endpoint interval8:37:31 and278/466chunks skipped. Parent82,983,739 plus two distinct report children6,356,626 gives89,340,365reconciled recorded coverage, not proven total usage. Originalmain4be3ce43 lacks currentASTRAledger/fixes and must notbeconfusedwithworktreeb2a2e10. Freshactual_current GraphBuilder concurrent-batch reproducer loses the committedfast episode receipt when a sibling timesout; batchreplayduplicatesfast extraction. RecommendASTRA06 durableepisode receipts+boundeddeadlines as the next majorworkflow slice afterUIentry, withidentity/invalidation/ambiguouswritecontracts coordinatedwith03b/03c. Audit files copied to docs/research/astra-successful-runs-audit-20260916.* andviz-market audit.

- 2026-09-16T11:22:48.216756+00:00: User explicitly said to stop Playwright and review the code. Browser automation is stopped; do not resume Playwright/CUA without a new explicit request. Keep the original UI objective and already-completed evidence, but focus ongoing work on source review, unit/integration regressions and workflow bottlenecks. Frontend-only preview remains on3000, backend5001 was not started; no user browser is closed. Root is tracing GraphitiRuntime batch timeouts and committed-episode receipt loss; two source specialists review async semantics and permanent-regression strategy. Launcher worker completed the independent-review P2 correction: frontend metadata proves the actual resolved /api and/health targets match the selected backend, so a different healthy DRF instance cannot be silently accepted. Source-only review and final gates precede commit.

- Source review correction completed: same-product health alone could accept an existing frontend routed to the wrong backend. Dev-only routing metadata now reads actual resolved proxy targets, exposes only safe loopback origins, and the launcher compares both targets with its selected backend. Independent source review approves43backend and5Node focused checks. Backend startup captures the launcher port before dotenv override; direct invocation behavior remains. Root updated README primaryroot/port/quiet-start instructions. Earlier browser checks are retained as historical UI evidence and will not be rerun after the user's stop instruction. Source/unit/integration work continues.
- Preview-only environment correction: shared node_modules font assets were outside Vite's filesystem allowlist. The external owned preview was narrowly configured to allow installed @fontsource assets plus its own frontend/cache directories. It now renders the fonts; backend5001 remains unstarted and readiness is truthfully unavailable. No production service was restarted.

- 2026-09-16T11:34:50.657211+00:00: Source-only graph review corrected the caller interpretation: GraphBuilder does not automatically retry a thrown batch; it marks the entire batch failed and continues. The external replay was explicit. Receipt loss can therefore overstate skipped chunks or falsely trigger all-text-failed; historical skipped counts do not prove all associated database writes are absent. The next coherent06a slice must preserve acknowledged input-index/UUIDs through the public sync timeout and accounting, and stop if cancellation cleanup is unresolved. Durable restart/input-policy checkpoints remain separate. Source anchors and acceptance are in docs/research/astra-graph-receipt-review.md.

- 2026-09-16T11:43:31.107727+00:00: Direct research workspace/readiness/launcher slice verified offline. Final backend3,957pass/1known scaffoldfail/17skip/11existingxfail;143guarded processes,zero network,source stable. Frontend131pass/build1.83s; all712built files match the previously browser-verified UI. Corrected independent-review finding prevents same-product/wrong-target frontend reuse. Source-only final review43backend+5Node cases has no blocker. Audit coversall26completed pipelines/156stage rows/254logs. Sources/evidence in docs/research/astra-workspace-verification.json and successful-runs-audit files. User instruction stops browser automation; source work continues with Graph receipts. Original checkout not deployed. Frontend-only3000preview remains; backend unstarted.
- Coordination correction: a send_message to an idle/completed agent only queued work, so the final routing gate and graph source review had not started. Switched to followup_task and obtained their completed results; no nonexistent validation is counted.

### ASTRA-06a acknowledged graph batch receipts — 2026-09-16T11:47:45.807217+00:00

The entry/UI slice is committed as 45a8da856f54159974fdff7f200e045de80c2357; user now requires source work and no browser automation. Current-code red tests exercise the real sync bridge/client/builder with deterministic barriers: one acknowledged UUID plus a hung sibling yields no delivered UUID and2failed/0succeeded. Both input orders fail; a write without acknowledgement remains unknown. GraphBuilder does not automatically replay a thrown batch.

Benefit hypothesis: retain acknowledged input-index/UUID receipts when the concurrent batch operation deadline expires, so GraphBuilder can report successful work accurately and avoid a false all-text-failed outcome. Record started/unacknowledged and never-started indexes separately. Take the immutable snapshot only after a bounded cancellation-cleanup acknowledgement from the submitted coroutine's outermost finally; an unconfirmed cleanup must stop the stage rather than allowing later reads/publication. Async cleanup does not cancel an executor/provider thread or prove rollback, so uncertainty remains explicit. Do not add automatic replay or durable caching in this slice.

Implementation design: a small thread-safe per-call receipt collector and typed batch-timeout outcome; capture acknowledgements inside the common ingest wrapper to cover initial fan-out and bounded serial replay; distinguish an already-completed future's inner TimeoutError from the actual sync deadline; preserve cleanup/lock boundaries and default behavior of other runtime operations. The public GraphNamespace must carry the typed outcome to GraphBuilder, which counts only validated acknowledged UUIDs as success and records remaining receipt gaps honestly. Keep total per-chunk attempts, cast-filter denominators, exact source text/reference time, seed validation and normal/serial behavior. A finite cancellation grace may be configured (small default, reject or safely bound non-finite/invalid values), documented and tested.

Acceptance: promote the three public-seam regressions, then cover timeout while queued/gathering/cooling down/replaying; both input orders, duplicate input names and empty UUIDs; unknown commit without acknowledgement; cooperative and uncooperative cleanup; inner timeout classification; no output/progress reorder or cross-graph leakage; unchanged successful-path bytes/call counts and actor-seed tests. Use only fake graph/provider operations. Compare baseline/current successful receipts and failed-chunk counts, not historical token savings. Full durable process-restart checkpoints, input/ontology-policy identities, storage failures and ambiguous-write reconciliation remain06b, coordinated with03b/03c.

- 2026-09-16T12:30:36.676096+00:00: ASTRA-06a is verified offline and independently approved. Immutable indexed receipts cross the actual sync runtime/facade/builder timeout boundary; started/unacknowledged and never-started work remain distinct. Bounded owned-async cleanup is required to continue, and unresolved cleanup stops before another batch or publication. No provider cancellation, rollback, automatic replay or durable restart is claimed. Both acknowledged input orders improve0→1returnedUUID and2→1recordedfailedinputs with unchanged fake-core calls; unknown writes remain0acknowledged success. Root and independent review reproduced a delivery/accounting race that doubled reasons; indexed already-accounted observations now preserve each final classification once, including childCancelledError.21permanent scenarios pass; focused144 andindependent109 pass. Final backend3978pass/1known scaffoldpathfailure/17skip/11existingxfail,143guarded processes,zero network/source drift, sixPythonfiles Ruff/compile andenvdocumentation gatepass. JUnitxunit1 onlyaddresses record-property serialization warnings. Exactsource/receipts/comparison: docs/research/astra-graph-receipt-verification.json.
- The successful-run audit was additionally checked for applicability: all26 completed pipeline states usefullmode, so the six-stage matrix does not misclassify research-only stages. The first optional check assumed state_source was a path; it is a wrapped source reference. Corrected the metadata lookup without changing any saved artifact.
- Next-session prompt: Keep browser automation stopped and continue source review/code work. Read docs/handoff/astra-experience/handoff.md, PLANS.md, ledger, all-successful-run audit andviz-market audit. SelectASTRA-MARKET-02 as the narrow identity prerequisite: preserve positional Yes/No token mapping when IDs are blank; exercise backend, bridge andlegacy paths with exact synthetic outcomes/token arrays beforefix. Then preserve research/current quote identity and per-row freshness01, reject invalid/disputed calibration resolution03, and repair nullable/out-of-range visual values. Do not trade correctness for assumed token savings. Inparallel read-onlydesign canprepare06b durable graph receipts/input-policy/ambiguous-write reconciliation, coordinatedwithontology03b/03c forkownership;06c per-episode deadlines remains separate. Userreported150M/currentrun cannotbeverifiedfromsavedJune–Julydata; do not repeat the old research subtotal into parent totals or treat loggedskips as provenabsentDBwrites. Originalmain stilllacksASTRAimplementation andmustnotbesilentlyoverwritten. No provider, realrun/resume, restarthealthyservices, deployment, secrets, savedartifacts or unchanged-auth retry.

- 2026-09-16T12:36:40.157195+00:00: Committed the source-only graph06a receipt/accounting repair and verified evidence as `4935dc3c58d61baf490eeec32127da49e1c2c2a7` (`fix: preserve acknowledged graph work across batch timeouts`). Earlier direct-entry/UI and all-successful-run audit commit is45a8da856f54159974fdff7f200e045de80c2357. The worktree was clean after each code commit. Loop remains ACTIVE; no browser automation resumes without explicit user request. Next source slice is binary market token identity, while06b durable recovery/06c deadlines and03b/03c input/ownership remain open. Remote publication remains blocked by unchanged authentication evidence.
