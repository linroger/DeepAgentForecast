{
  "name": "deepagentforecast",
  "version": "0.2.0",
  "description": "DeepAgentForecast — one prompt: deep research → knowledge graph → multi-agent simulation → forecast",
  "scripts": {
    "setup": "npm install && cd frontend && npm install",
    "setup:backend": "cd backend && uv sync --python 3.12",
    "setup:all": "npm run setup && npm run setup:backend",
    "doctor": "bash scripts/doctor.sh",
    "smoke": "bash scripts/smoke.sh",
    "dev": "concurrently --kill-others -n \"backend,frontend\" -c \"green,cyan\" \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && uv run python run.py",
    "frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "test": "cd backend && uv run pytest",
    "lint": "cd backend && uv run ruff check app",
    "check:env": "cd backend && uv run python scripts/check_env_drift.py --strict"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "license": "AGPL-3.0"
}
