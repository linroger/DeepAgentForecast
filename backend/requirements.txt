# ===========================================
# MiroFish Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= 核心框架 =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM 相关 =============
# OpenAI SDK（统一使用 OpenAI 格式调用 LLM）
openai>=1.0.0

# ============= Zep Cloud =============
zep-cloud==3.13.0

# ============= OASIS 社交媒体模拟 =============
# OASIS 社交模拟框架
camel-oasis==0.2.5
camel-ai==0.2.78

# ============= 文件处理 =============
PyMuPDF>=1.24.0
# 编码检测（支持非UTF-8编码的文本文件）
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= 工具库 =============
# 环境变量加载
python-dotenv>=1.0.0

# 数据验证
pydantic>=2.0.0

# ============= 报告可视化（VIZ-1）=============
# 确定性报告图表渲染（Agg 无头后端生成 PNG）。可选依赖：未安装时可视化器自动降级为
# 仅 Mermaid 模式（见 app/services/report_visualizer.py 的 MATPLOTLIB_AVAILABLE 标志）。
matplotlib>=3.7.0

# ============= MCP 服务器（ITEM 4）=============
# 官方 Model Context Protocol Python SDK（FastMCP）。app/mcp/{kg_server,sim_server}.py 以
# stdio 传输把 KG 检索与模拟采访暴露给 deer-flow 2.0 harness。仅这两台 MCP 服务器进程需要它；
# 主后端不 import 本包（构建/列工具在 build_server 内懒导入），故缺失时只影响 MCP 暴露、不影响主流程。
mcp>=1.10.0
# ITEM-16：交互式 HTML 图表族（plotly.js 内联、离线自包含）。可选依赖：未安装时 HTML 族
# 自动跳过（见 report_visualizer.py 的 PLOTLY_AVAILABLE 标志），PNG/Mermaid 族不受影响。
plotly>=5.18.0
